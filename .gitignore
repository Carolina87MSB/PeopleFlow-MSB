# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Mapeamento local (nome -> matrícula/depto_code/nivel/gestor/admissao) usado
# por scripts/seed-supabase.mjs para popular os campos do PeopleFlow na tabela
# `colaboradores` compartilhada com o Portal SST. Repositório é PÚBLICO —
# nunca commitar dado real. Ver src/data/colaboradores.example.json.
src/data/colaboradores.local.json

# SQL com nomes reais de colaboradores gerado sob demanda (ex.: a partir de
# planilhas de RH) para rodar manualmente no Supabase Dashboard. Nunca
# commitar — repositório é PÚBLICO.
supabase/*.local.sql

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
